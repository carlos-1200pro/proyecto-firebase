# proyecto-firebase
Proyecto web estático con Nginx y Docker, para despliegue en Firebase.
# Proyecto Firebase 🚀

Sitio web estático "Hola Mundo Avanzado" desplegado con Nginx dentro de Docker.

## Contenido
- HTML, CSS, JS básicos
- Servidor Nginx con Dockerfile
- Compatible con Firebase Hosting

## Cómo ejecutar localmente

```bash
docker compose up --build -d
